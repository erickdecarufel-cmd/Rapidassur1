# Settings to manage and conﬁgure a Firebase App Hosting backend.
# https://ﬁrebase.google.com/docs/app-hosting/conﬁgure

runConﬁg:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
