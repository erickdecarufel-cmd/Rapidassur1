'use client';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function OccurrenceClaimsMadeQuiz() {
    return (
        <Card className="border-accent">
            <CardHeader><CardTitle className="text-accent">Quiz — Occurrence vs Claims-Made</CardTitle></CardHeader>
            <CardContent><p className="text-muted-foreground">Quiz interactif en cours de développement.</p></CardContent>
        </Card>
    );
}