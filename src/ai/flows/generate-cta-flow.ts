'use server';
/**
 * @fileOverview Un agent IA qui génère un appel à l'action (CTA) personnalisé. */  import { ai } from '@/ai/genkit'; import { z } from 'zod';  const GenerateCtaInputSchema = z.object({ profileTitle: z.string().describe('Le titre du profil professionnel.'), profileDescription: z.string().describe('La description détaillée du profil professionnel.'), }); export type GenerateCtaInput = z.infer<typeof GenerateCtaInputSchema>;  const GenerateCtaOutputSchema = z.object({ cta: z.string().describe("L'appel à l'action personnalisé et engageant."), }); export type GenerateCtaOutput = z.infer<typeof GenerateCtaOutputSchema>;  export async function generateCta(input: GenerateCtaInput): Promise<GenerateCtaOutput> { return generateCtaFlow(input); }  const prompt = ai.definePrompt({ name: 'generateCtaPrompt', input: { schema: GenerateCtaInputSchema }, output: { schema: GenerateCtaOutputSchema }, prompt: `Tu es un expert en marketing pour une compagnie d'assurance nommée RapidAssur.
Ton objectif est de générer un appel à l'action (CTA) court, percutant et rassurant pour un professionnel.  Le CTA doit être une seule phrase et ne doit pas dépasser 150 caractères. Il doit encourager l'utilisateur à cliquer sur le bouton \"Je démarre\" pour obtenir un diagnostic personnalisé. Utilise un ton professionnel, mais accessible et engageant. Mets en avant le bénéfice principal pour le client.  Voici le profil du professionnel : Titre du profil : {{profileTitle}} Description : {{profileDescription}}  Génère uniquement le texte du CTA.`, });  const generateCtaFlow = ai.defineFlow( { name: 'generateCtaFlow', inputSchema: GenerateCtaInputSchema, outputSchema: GenerateCtaOutputSchema, }, async (input) => { const { output } = await prompt(input); return output!; } );